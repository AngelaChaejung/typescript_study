function 기계(여기) {
  this.q = 여기; //새로생성된object의 q
  this.w = "snowball";
}

let nunu = new 기계("consume");
let garen = new 기계("strike");

console.log(기계);

class Hero {
  constructor(여기) {
    this.q = 여기; //새로생성된object의 q
    this.w = "snowball";
  }
}
